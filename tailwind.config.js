/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{js,jsx,ts,tsx}', // Includes all JS, JSX, TS, and TSX files in the src directory and subdirectories
    './public/**/*.html', // Includes all HTML files in the public directory and subdirectories
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
